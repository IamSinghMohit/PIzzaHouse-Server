import {Request,Response,NextFunction} from "express"

class ProductRead {
    static async getProduct(
        req: Request,
        res: Response,
        next: NextFunction
    ){

    }
}
export default ProductRead 